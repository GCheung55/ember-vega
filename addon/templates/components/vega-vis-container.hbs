{{yield
    (hash
        element=element
        dimensions=dimensions
        vis=(component
            "vega-vis"
            height=dimensions.height
            width=dimensions.width
            tagName=""
            visContainer=element
        )
    )
}}